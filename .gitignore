# Gradle
.gradle/
.kotlin/
build/
**/build/

# Local Android SDK configuration
local.properties

# Android Studio / IntelliJ IDEA
.idea/
*.iml

# Local coding-agent state
.claude/

# Local planning and agent instructions
/AGENTS.md
/CLAUDE.md
/docs/

# Native build output
.externalNativeBuild/
.cxx/

# OS files
.DS_Store
Thumbs.db

# Signing and secrets
*.jks
*.keystore
*.p12
*.pfx
keystore.properties
internal-signing.properties
internal-signing-record.*
internal-signing-fingerprint.*
internal-signing-custody.*
internal-signing-recovery.*
**/google-maps.properties
# V02-008: common names for secret carriers; none of these is a TrailVeil input today, and the
# only key the build reads lives outside the checkout, so a copy here can only be a mistake.
secrets.properties
.env
.env.*
google-services.json

# Local device-capture helpers for a specific investigation; never part of the product.
capture-p4-029.ps1
build-debug-apk.ps1
